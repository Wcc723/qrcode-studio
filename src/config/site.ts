export const site = {
  name: 'QRTool',
  // 正式網域（無結尾斜線）；用於 canonical / og:url / sitemap / 結構化資料
  url: 'https://qrcode-studio.pocketool.app',
  defaultLocale: 'zh-TW',
  // 預設社群分享圖（站內路徑，建議 1200×630）。圖檔放進 public/ 後填入此值即會輸出 og:image / twitter:image；留空則不輸出，避免指向不存在的圖。
  ogImage: '',
  description: '免費線上 QR Code 產生器，3 步驟製作 QRCode：瀏覽器內即時生成、不上傳，可自訂顏色與加入 LOGO，免費下載 PNG 與 SVG 向量檔，永久有效不過期。',
}
