# 變更紀錄

本檔記錄使用者感知得到的改版。版本號與 `package.json` 的 `version` 一致，
tag 在部署成功且線上驗過之後才打。

## [1.4.0] - 2026-09-26

### 變更

- **工具與說明分開了。** 首頁、7 個類型頁、掃描器與一維條碼的上半部是工具區（淡薄荷色底），
  下面是說明文章，改成像文件一樣好讀的樣式；工具卡以外不再用粗框卡片，一眼分得出哪裡能操作、
  哪裡是說明。
- **工具裡的字變少了。** 標題下只留一行副標，原本那段完整說明移到下方文章開頭。長說明收進欄名
  旁的「?」，點了才展開；錯誤、警告與填寫規則仍然一直看得到。
- 產生器的容錯等級、LOGO、尺寸、顏色與各類型的輸入欄，第一次有了說明：例如容錯等級 L、M、Q、H
  各能還原多少、LOGO 會蓋掉多少、1000 像素大約能印多大。
- 「只在你的瀏覽器產生，內容不上傳」合併成工具卡頂部一行，完整說明與隱私權政策連結收在「?」裡，
  不再在標題、預覽框各重複一次。
- 首頁的 7 張類型卡移到下方說明裡，改成附圖示的連結列。
- 畫面上的 emoji 全部換成單色圖示，放進糖果色的圓底徽章；圖示在建置時就打包進網站，瀏覽器不會
  向任何第三方取圖。
- 連結改成深一點的橘色，在淺色底上更容易看清楚。

### 修正

- **勾選「透明」再下載 JPG，整張背景變成黑色、掃不出來。** JPG 格式沒有透明，現在會改用白色背景；
  要透明背景請下載 PNG 或 SVG，勾選透明時畫面上也會提醒。
- 卡片、按鈕與頁首頁尾原本設計的粗黑框沒有畫出來（只剩陰影），一維條碼的類型選項看起來像純文字，
  現在都恢復成設計的樣子。
- 桌機填寫電子名片這類較長的表單時，右邊的預覽會跟著捲走，現在會停在畫面上方。

### 技術

- 新增 Lucide 圖示（ISC 授權，由 Feather 衍生的部分為 MIT），授權寫在 `NOTICE.md` 與關於頁。
- 新增護欄：畫面文案與元件不得出現 emoji、每個「?」預設收起而且欄位指得到它、錯誤與警告不得被收起；
  建置時的稽核另外驗證原本標題下的說明句仍在預渲染 HTML 的說明文章裡、用到的圖示都有打包。

## [1.3.0] - 2026-09-21

### 修正

- **內容含中文或 emoji 的 QR Code，掃出來是亂碼。** 純文字、電子名片的中文欄位、
  中文的 WiFi 名稱或密碼、中文簡訊，以及網址裡直接打的中文字，只要內容有中文、emoji
  這類非英文字元，產生的 QR Code 掃出來都是亂碼。現在改以通行的 UTF-8 編碼，掃出來就是
  原本的文字。
  - **在這次修正之前產生、內容含中文或 emoji 的 QR Code，請重新產生一張替換**，
    尤其是已經印在名片、貼紙、菜單或海報上的，建議先用手機實際掃過確認。
  - 內容只有英文字母、數字與符號的（例如一般網址），產生的圖案與修正前完全相同，
    不必重做。Email 類型的主旨與內文原本就轉成網址編碼，一維條碼產生器也用不同的
    編碼器，兩者都不受影響。
- 教學文章與 7 個類型頁的說明本文整段出現底線、變成等寬字，上傳 LOGO 的欄位整片
  變成黃色：樣式設定沒有正確展開，已恢復成原本設計的樣子。
- 網址打錯的教學文章與大小寫不對的網址（例如 `/WIFI/`），現在會正確顯示「找不到
  頁面」。之前前者會讓頁面出錯，後者會顯示成對應的工具頁。
- 移除頁面上「廣告版位（待 AdSense 審核啟用）」的虛線佔位框。

### 新增

- **教學總覽**（`/qrcode-studio/guide/`）：6 篇教學依「入門」「製作與印刷」「應用」
  分組列出，附最後更新日期；教學文章頁加上看得到的麵包屑，可以回到總覽。
- 6 篇教學全面改寫並加長，附上實際產生的示意圖：QR Code 構造圖、同一個網址在
  L／M／Q／H 四種容錯等級的對照、PNG 放大與 SVG 的對照、靜區示意等，圖裡的 QR Code
  都掃得出來。文章標示作者與最後更新日期，文末附相關教學。
- 常見問題（`/qrcode-studio/faq/`）改寫成 24 題，分成費用、隱私、外觀與 LOGO、
  下載與印刷、類型、掃描六組，附分類跳轉。
- 7 個 QR Code 類型頁改放各自專屬的常見問題，例如網址的 UTM 參數、電話的 +886
  國際格式、簡訊在 iPhone 與 Android 的差異，並加上延伸閱讀與其他類型、工具的連結。
- 掃描器與一維條碼產生器有各自的分享預覽圖；網站加上瀏覽器分頁圖示。

### 變更

- 工具名稱統一為 **QR Code Studio**，不再出現舊名 QRTool。
- 還沒輸入內容時，預覽區改顯示一張淡色、標著「示範」的裝飾圖（掃不出內容）。
- 頁首與頁尾加上教學、常見問題與口袋工具的連結；頁尾分成 QR Code 類型、其他工具、
  教學、關於四組。
- 隱私權政策註明同時適用口袋工具全站的政策，補上第三方廣告 Cookie 的說明與停用方式；
  關於頁補上營運者、三個工具的運作方式與更新紀錄。

### 技術

- 新增 UTF-8 往返測試：用產生器實際使用的 QR Code 編碼器產生，再用掃描器的解碼器
  讀回，中文、電子名片、WiFi、emoji 簡訊都必須讀回原文。
- 教學本文改為只在文章頁動態載入，不進入每一頁都會下載的 entry chunk。
- 預設分享圖由 864 KB 壓到 201 KB。
- 建置時的 SEO 稽核新增多條逐頁檢查（未展開的樣式 class、佔位框文字、圖片路徑的
  子路徑前綴、分享圖大小、網站圖示、JSON-LD 類型、教學的日期與作者等），不符就中止建置。

## [1.2.0] - 2026-09-20

### 新增

- **QR Code 與條碼圖片掃描器**（`/qrcode-studio/scan/`）：選取檔案、拖放或直接貼上
  一張圖片，在瀏覽器內讀出內容。
  - 支援 QR Code，以及 Code 128、EAN-13、EAN-8、Code 39、ITF-14 五種一維條碼。
  - 圖片支援 PNG、JPEG 與 WebP，一次一張；不接受 SVG 與多個檔案。
  - QR Code 的內容會辨識成網址、WiFi、電子名片、Email、電話、簡訊或純文字分別顯示。
  - 可以把讀出來的內容帶回 QR Code 或一維條碼產生器，重新產生一張。

### 隱私與安全

- 圖片與解讀出來的內容只留在瀏覽器記憶體，不寫進網址、Cookie 或瀏覽器的各種儲存空間，
  也不會上傳。
- 解碼器（WebAssembly）由本站自己提供，不從第三方 CDN 載入，而且只有在 `/scan/`
  選了圖片之後才會下載。
- 掃出來的網址不會自動開啟，未知或有風險的網址格式不提供直接前往的連結；內容一律以
  純文字顯示，不會被當成網頁執行。

### 變更

- 首頁、頁尾與「如何掃描 QR Code」教學加入掃描器的連結；隱私權政策、關於頁與
  第三方授權說明（`NOTICE.md`）同步更新。

### 技術

- `zxing-wasm` 精確鎖定 `3.1.4`，`NOTICE.md` 記錄上游授權、實際安裝的版本與 WASM 雜湊。

## [1.1.0] - 2026-09-13

### 新增

- **一維條碼產生器**（`/qrcode-studio/barcode/`）：支援 Code 128、EAN-13、EAN-8、
  Code 39、ITF-14 五種格式，即時預覽，下載 PNG 與 SVG。
  - EAN-13、EAN-8、ITF-14 會自動計算 GS1 mod-10 檢查碼：輸入不含檢查碼的位數會補上並
    顯示完整號碼，輸入完整位數則驗證檢查碼，不符時明確指出正確的那一位是多少。
  - 依各規範算出靜區（EAN 系列左右不對稱）與 ITF-14 的外框 bearer bars，
    這兩項缺少時畫面上看不出來，但印出來會掃不到。
  - SVG 下載帶 mm 實體尺寸，置入排版軟體即為標稱尺寸（EAN-13 為 37.29 mm）。
    PNG 檔案格式無法記錄每英吋點數，所以要精確尺寸請用 SVG。
  - PNG 提供網頁用、列印、高解析印刷三段解析度，並顯示換算後的 DPI。
  - 條碼本體固定純黑白，刻意不提供顏色、漸層與 LOGO：一維條碼沒有錯誤更正碼，
    而且紅光掃描器看不見紅色的條。

### 變更

- 首頁、頁尾與「什麼是 QR Code」教學各新增一條指向一維條碼產生器的連結，
  並說明一維條碼與 QR Code 的用途差異。

### 技術

- 條碼編碼器自行實作（`src/pure/encodeBarcode.ts`），只走 `/barcode/` 路由動態載入，
  未進入 entry chunk：entry 的 gzip 僅由 94,557 增加到 94,883（+0.34%，來自新增的
  互鏈文案），條碼相關程式碼全部在獨立的 10.4 KB gzip chunk 內。
- 所有位元樣式以獨立的 ZXing C++ 解碼器回讀驗證，五種格式的 PNG 與 SVG 產物
  都能被讀回完全相同的內容。
