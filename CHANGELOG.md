# 變更紀錄

本檔記錄使用者感知得到的改版。版本號與 `package.json` 的 `version` 一致，
tag 在部署成功且線上驗過之後才打。

## [1.1.0] - 2026-09-13

### 新增

- **一維條碼產生器**（`/qrcode-studio/barcode/`）：支援 Code 128、EAN-13、EAN-8、
  Code 39、ITF-14 五種格式，即時預覽，下載 PNG 與 SVG。
  - EAN-13、EAN-8、ITF-14 會自動計算 GS1 mod-10 檢查碼：輸入不含檢查碼的位數會補上並
    顯示完整號碼，輸入完整位數則驗證檢查碼，不符時明確指出正確的那一位是多少。
  - 依各規範算出靜區（EAN 系列左右不對稱）與 ITF-14 的外框 bearer bars，
    這兩項缺少時畫面上看不出來，但印出來會掃不到。
  - SVG 下載帶 mm 實體尺寸，置入排版軟體即為標稱尺寸（EAN-13 為 37.29 mm）。
    PNG 檔案格式無法記錄每英吋點數，所以要精確尺寸請用 SVG。
  - PNG 提供網頁用、列印、高解析印刷三段解析度，並顯示換算後的 DPI。
  - 條碼本體固定純黑白，刻意不提供顏色、漸層與 LOGO：一維條碼沒有錯誤更正碼，
    而且紅光掃描器看不見紅色的條。

### 變更

- 首頁、頁尾與「什麼是 QR Code」教學各新增一條指向一維條碼產生器的連結，
  並說明一維條碼與 QR Code 的用途差異。

### 技術

- 條碼編碼器自行實作（`src/pure/encodeBarcode.ts`），只走 `/barcode/` 路由動態載入，
  未進入 entry chunk：entry 的 gzip 僅由 94,557 增加到 94,883（+0.34%，來自新增的
  互鏈文案），條碼相關程式碼全部在獨立的 10.4 KB gzip chunk 內。
- 所有位元樣式以獨立的 ZXing C++ 解碼器回讀驗證，五種格式的 PNG 與 SVG 產物
  都能被讀回完全相同的內容。
